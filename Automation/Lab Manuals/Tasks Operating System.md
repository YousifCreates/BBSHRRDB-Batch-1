# Tasks: Operating System (os) modules using automation:

### 1. Create a python function `create_folder_structure(project_path)` that creates the project folder named `First_Automation_Project` and having the following structure:

```
First_Automation_Project/
  |__Code/
      |__ app.py
      |__ services.py
      |__ login.py
      |__ register.py
  |__Images/
  |__API/
      |__routes/
      |__main.py
  |__public/
```

**Note: Run the file and execute the `create_folder_structure(project_path)` in the `main()` function in python.Do not use any AI resources, do it by yourself.**
      
